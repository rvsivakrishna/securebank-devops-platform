apiVersion: v1
kind: Service

metadata:
  name: securebank-service
  namespace: securebank

spec:
  type: NodePort

  selector:
    app: securebank

  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
